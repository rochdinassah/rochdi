#!/bin/bash

# author: rochdi nassah
# created: 2025/09/19

declare -A aliases

aliases[desktop]='cd ~/Desktop'

while read line
do
  for alias in ${!aliases[@]}
  do
    if [ "$line" == 1 ]
    then
      echo found
      # break
    fi
  done
done < ~/.bashrc