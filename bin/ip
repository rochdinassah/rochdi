#!/usr/bin/env node

const rochdi = require('../');

const { Http2Client } = rochdi;

const http2Client = new Http2Client();

const headers = {
  'user-agent': 'curl'
};

http2Client.get('https://ifconfig.co', { headers, keepalive: false }).then(res => {
  log(res.data.trim());
});