#!/usr/bin/env node

const rochdi = require('../');

const { Http2Client } = rochdi;

const http2Client = new Http2Client();

http2Client.get('https://ifconfig.co', { headers: { 'user-agent': 'curl' } }).then(res => {
  log(res.data.trim());
  http2Client.destroy();
});