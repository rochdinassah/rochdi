#!/usr/bin/env node

'use strict';

const rochdi = require('../');
const crypto = require('node:crypto');
const tls = require('node:tls');

const { HttpClient, formatDuration, rand, randomString, getTime } = rochdi;
const { Http2Client } = rochdi;

const http2Client = new Http2Client()

log(http2Client.createSession('https://google.com', { keepalive: true }));